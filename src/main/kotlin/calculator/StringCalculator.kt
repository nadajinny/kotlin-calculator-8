package calculator

class StringCalculator {
    fun run(input: String): Int {
        //빈 문자열 처리
        if(input.isEmpty()) return 0
        if(input.isBlank()) return 0

        val sum = 1
        return sum
    }
}